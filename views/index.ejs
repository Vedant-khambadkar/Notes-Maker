<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="main w-full min-h-screen bg-zinc-900 ">
        <div class="form p-10 text-white">
            <h1 class="font-bold text-[3.5vw] text-center mb-10">CREATE YOUR OWN NOTES</h1>
            <form action="/create" method="post">

                <input class="block mt-3 w-full  outline-none rounded-lg px-5 py-2 bg-zinc-700" type="text"
                    placeholder="Enter the file name" name="title">
                <textarea
                    class="block mt-3 w-full outline-none rounded-lg resize-none bg-zinc-700 px-5 py-2 bg-zink-600"
                    placeholder="Enter kuch bhi" name="details"></textarea>
                <input  class="mt-3 bg-blue-400 p-2 rounded-lg font-semibold" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 text-white flex gap-5 flex-wrap">
            <% if(files.length >0){ %>
                
                <% files.forEach((val)=>{ %>
                    <div class="task  w-72 px-3 rounded-lg py-4 bg-zinc-700">
                        <h1 class=" text-2xl tracking-tighter"><%= val %></h1>
                        <div class=" flex justify-between mt-4">
                            
                            <a class="text-blue-500 inline-lock" href="/files/<%=val %>">Read more</a>
                            <a class="text-zinc-400 inline-lock" href="./edit/<%=val %>">Edit FileName</a>
                            <a class="text-red-400 inline-lock" href="./delet/<%=val %>">Delet</a>
                           
                        </div>
                    </div>
             <%   })%>
         <%   }else{ %>
                <div class=" text-zinc-400">
                    <h3>No task here...</h3>
                </div>
        <%    } %>

                          
                          
                          
        </div>
    </div>
    <script src="/javascripts/script.js"></script>
</body>

</html>